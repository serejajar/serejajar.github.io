Первое, что нужно сделать это чуть подправить экспорт:

export default calculate;
затем в папке задания выполните команду:

npm test


-------

Нет, вы меня неправильно поняли. Вам нужно исправить:

if (arrayBlacklist.includes(email) === false) {
На

if (!arrayBlacklist.includes(email)) {
В обоих случаях код будет работать одинаково, но первый способ делает двойную работу, которую вам нужно избегать в коде.


---------------
Есть одно небольшое исправление по коду:

if (secondlist.includes(firstList[i])) {
} else {
  validList.push(firstList[i]);
}
Нужно исправить используя оператор НЕ "!".

---------------
Посмотрите на верхнюю строчки на вашем скриншоте где указано что ожидается (expected) и что пришло на самом деле (received). Вы сразу увидите отличие и причину почему тесты падают.

---------------

Перенесите код для каждого задания в соответсвующий ему файл чтобы я можно было запустить тесты функций. Код для корзины в файл cart-total.js и т.д.
